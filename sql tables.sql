/create table

CREATE TABLE book(
				id int NOT NULL AUTO INCREMENT,
				title varchar(250),
				author varchar(250),
				price int,
				PRIMARY KEY(ID)
				);
				

DESC TABLE =VIEWS COLUMN AND CONTENTS OF SAME.

SELECT * FROM book= give output of all the books in te current dadabase.

// POPULATE TABLE.

INSERT INTO book(title, author, price) values ('Christmas', 'Jamie Oliver', 20);

